<!--
  Copyright (C) 2026 Max Dietrich
  Licensed under the GNU AGPLv3. See LICENSE in the project root for details.
-->

<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Basic location permissions -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Background location (Android 10+) -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    
    <!-- Foreground service permission (Android 9+) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    
    <!-- Foreground service type (Android 10+) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <!-- Notification permission (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Network permissions -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_LOCAL_NETWORK" />

    <!-- Boot Receiver -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- Held during the stationary heartbeat's fix acquisition; also used by WorkManager -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:name=".MainApplication"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:theme="@style/AppTheme"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustPan"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="huttstracking" />
            </intent-filter>
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>

        <!-- Handles app shortcut intents without showing UI -->
        <activity
            android:name=".triggers.ShortcutHandlerActivity"
            android:theme="@android:style/Theme.NoDisplay"
            android:exported="true"
            android:excludeFromRecents="true" />

        <!-- Foreground location service -->
        <service
            android:name=".service.LocationForegroundService"
            android:enabled="true"
            android:exported="false" 
            android:foregroundServiceType="location"
        >
            <intent-filter>
                <action android:name="com.huttsmedia.huttstracking.RECHECK_PAUSE_ZONE" />
                <action android:name="com.huttsmedia.huttstracking.ACTION_MANUAL_FLUSH" />
            </intent-filter>
        </service>

        <!-- WorkManager foreground service (auto-export) -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            android:exported="false" />

        <!-- Backup/Restore foreground service -->
        <service
            android:name=".backup.BackupForegroundService"
            android:foregroundServiceType="shortService"
            android:exported="false" />

        <!-- Tracking restart receiver (boot / app update) -->
        <receiver
            android:name=".triggers.LocationBootReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <category android:name="android.intent.CATEGORY_DEFAULT" />
            </intent-filter>
        </receiver>

        <!-- Auto-export alarm receiver -->
        <receiver
            android:name=".export.AutoExportAlarmReceiver"
            android:enabled="true"
            android:exported="false" />

        <!-- Stationary heartbeat alarm receiver -->
        <receiver
            android:name=".service.StationaryHeartbeatReceiver"
            android:enabled="true"
            android:exported="false" />

        <!-- Geofence heartbeat alarm receiver -->
        <receiver
            android:name=".service.GeofenceHeartbeatReceiver"
            android:enabled="true"
            android:exported="false" />

        <!-- Tracking liveness watchdog alarm receiver -->
        <receiver
            android:name=".service.TrackingWatchdogReceiver"
            android:enabled="true"
            android:exported="false" />

        <!-- Tracking control receiver (Tasker / automation) -->
        <receiver
            android:name=".triggers.TrackingControlReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="com.huttsmedia.huttstracking.action.START_TRACKING" />
                <action android:name="com.huttsmedia.huttstracking.action.STOP_TRACKING" />
            </intent-filter>
        </receiver>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
